export interface SingleBookmarkData {
  title: string;
  color: string;
  column: number;
  priority: number;
  linksTitles: string[];
}

export interface SingleLinkData {
  title: string;
  URL: string;
  tags: string[]
}

